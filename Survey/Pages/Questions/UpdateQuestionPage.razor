@page "/questions/update/{id}"

<UpdateQuestionComponent id="@id" />

@code {
    [Parameter]
    public string id { get; set; }

    protected override async Task OnInitializedAsync()
    {
        id = id;
    }
}
